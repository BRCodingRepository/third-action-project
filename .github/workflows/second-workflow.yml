name: Issues Details
on: workflow_dispatch
jobs:
  issue:
    runs-on: ubuntu-latest
    steps:
      - name: Issue Details
        run: |
          echo "=== Complete GitHub Event Context ==="
          echo "${{toJSON(github.event)}}"
